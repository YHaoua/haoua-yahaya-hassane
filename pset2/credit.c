#include <stdio.h>
#include <cs50.h>
#include <ctype.h>
#include <string.h>

int main (void)
{
    int sum=0;
    long long cc;

    do
    {
        cc=get_long_long("your card number: ");

    }
    while (cc<o);

    int count=0;

    long long number=cc;

    while (ccdigit>0)
    {
        ccdigit=ccdigit/10;
        count++;
        if (count!=13 && count!=15 && count!=16)
        {
            printf("this is a invalid card number\n");

        }
         int digit[count];
        for (int i=0; i<count; i++)
{
    digit[i]=cc%10;
    cc=cc/10;

    int odigit [count];
    for (int i=0; i<count; i++)


}
    }
}